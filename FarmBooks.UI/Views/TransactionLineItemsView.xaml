<UserControl x:Class="FarmBooks.UI.Views.TransactionLineItemsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel>
        <DockPanel Margin="0,12,0,8">
            <TextBlock Text="Line Items"
                       FontSize="18"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"
                       DockPanel.Dock="Left"/>

            <StackPanel Orientation="Horizontal"
                        DockPanel.Dock="Right">

                <Button Content="Add Line"
                        Width="90"
                        Height="30"
                        Margin="0,0,8,0"
                        Command="{Binding AddLineItemCommand}"/>

                <Button Content="Remove Line"
                        Width="100"
                        Height="30"
                        Command="{Binding RemoveLineItemCommand}"/>
            </StackPanel>
        </DockPanel>

        <DataGrid ItemsSource="{Binding LineItems}"
                  SelectedItem="{Binding SelectedLineItem}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  MinHeight="160">

            <DataGrid.Columns>
                <DataGridComboBoxColumn Header="Code"
                                        SelectedValueBinding="{Binding CodeId, UpdateSourceTrigger=PropertyChanged}"
                                        SelectedValuePath="CodeId"
                                        DisplayMemberPath="DisplayName"
                                        Width="180">

                    <DataGridComboBoxColumn.ElementStyle>
                        <Style TargetType="ComboBox">
                            <Setter Property="ItemsSource"
                                    Value="{Binding DataContext.AccountingCodes,
                                            RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                        </Style>
                    </DataGridComboBoxColumn.ElementStyle>

                    <DataGridComboBoxColumn.EditingElementStyle>
                        <Style TargetType="ComboBox">
                            <Setter Property="ItemsSource"
                                    Value="{Binding DataContext.AccountingCodes,
                                            RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                            <Setter Property="IsEditable" Value="True"/>
                            <Setter Property="Text"
                                    Value="{Binding AccountingCode,
                                            UpdateSourceTrigger=LostFocus}"/>
                        </Style>
                    </DataGridComboBoxColumn.EditingElementStyle>
                </DataGridComboBoxColumn>

                <DataGridTextColumn Header="Description"
                                    Binding="{Binding Description,
                                              UpdateSourceTrigger=PropertyChanged}"
                                    Width="*"/>

                <DataGridTextColumn Header="Total"
                                    Binding="{Binding Amount,
                                              UpdateSourceTrigger=PropertyChanged,
                                              StringFormat=F2}"
                                    Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</UserControl>
