<UserControl x:Class="FarmBooks.UI.Views.ExpenseLineItemsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel>
        <DockPanel Margin="0,12,0,8">
            <TextBlock Text="Line Items"
                       FontSize="18"
                       FontWeight="SemiBold"
                       DockPanel.Dock="Left" />

            <Button Content="Add Line"
                    Width="90"
                    Height="30"
                    DockPanel.Dock="Right"
                    Command="{Binding AddLineItemCommand}" />
        </DockPanel>

        <DataGrid ItemsSource="{Binding LineItems}"
                AutoGenerateColumns="False"
                CanUserAddRows="False"
                CanUserDeleteRows="True"
                GridLinesVisibility="Horizontal"
                HeadersVisibility="Column"
                MinHeight="160">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Description"
                                    Binding="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                                    Width="*" />

                <DataGridComboBoxColumn Header="Accounting Code"
                                        SelectedItemBinding="{Binding AccountingCode, UpdateSourceTrigger=PropertyChanged}"
                                        Width="150">
                    <DataGridComboBoxColumn.ElementStyle>
                        <Style TargetType="ComboBox">
                            <Setter Property="ItemsSource"
                                    Value="{Binding DataContext.AccountingCodes, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                        </Style>
                    </DataGridComboBoxColumn.ElementStyle>

                    <DataGridComboBoxColumn.EditingElementStyle>
                        <Style TargetType="ComboBox">
                            <Setter Property="ItemsSource"
                                    Value="{Binding DataContext.AccountingCodes, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                        </Style>
                    </DataGridComboBoxColumn.EditingElementStyle>
                </DataGridComboBoxColumn>

                <DataGridTextColumn Header="Amount"
                                    Binding="{Binding Amount, UpdateSourceTrigger=PropertyChanged, StringFormat=F2}"
                                    Width="100" />
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</UserControl>