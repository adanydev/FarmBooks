<UserControl x:Class="FarmBooks.UI.Views.TransactionListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <Style x:Key="StatusBadgeBorderStyle"
               TargetType="Border">
            <Setter Property="MinWidth"
                    Value="34"/>
            <Setter Property="Height"
                    Value="22"/>
            <Setter Property="Padding"
                    Value="7,1"/>
            <Setter Property="CornerRadius"
                    Value="11"/>
            <Setter Property="HorizontalAlignment"
                    Value="Center"/>
            <Setter Property="VerticalAlignment"
                    Value="Center"/>
            <Setter Property="Background"
                    Value="#FEF3C7"/>
            <Setter Property="BorderBrush"
                    Value="#F59E0B"/>
            <Setter Property="BorderThickness"
                    Value="1"/>
        </Style>

        <Style x:Key="StatusBadgeTextStyle"
               TargetType="TextBlock">
            <Setter Property="Foreground"
                    Value="#92400E"/>
            <Setter Property="FontWeight"
                    Value="SemiBold"/>
            <Setter Property="HorizontalAlignment"
                    Value="Center"/>
            <Setter Property="VerticalAlignment"
                    Value="Center"/>
        </Style>

        <Style x:Key="DetailHeaderTextStyle"
               TargetType="TextBlock">
            <Setter Property="FontSize"
                    Value="11"/>
            <Setter Property="FontWeight"
                    Value="SemiBold"/>
            <Setter Property="Foreground"
                    Value="#4B5563"/>
            <Setter Property="Margin"
                    Value="4,0"/>
        </Style>

        <Style x:Key="DetailValueTextStyle"
               TargetType="TextBlock">
            <Setter Property="Margin"
                    Value="4,5"/>
            <Setter Property="VerticalAlignment"
                    Value="Center"/>
            <Setter Property="TextWrapping"
                    Value="Wrap"/>
        </Style>
    </UserControl.Resources>

    <DataGrid ItemsSource="{Binding TransactionList.FilteredTransactions}"
              SelectedItem="{Binding SelectedTransaction}"
              AutoGenerateColumns="False"
              CanUserAddRows="False"
              IsReadOnly="True"
              SelectionMode="Single"
              SelectionUnit="FullRow"
              GridLinesVisibility="Horizontal"
              HeadersVisibility="Column"
              AlternatingRowBackground="#F9FAFB"
              BorderThickness="0"
              RowDetailsVisibilityMode="Collapsed"
              Tag="{Binding DataContext,
                            RelativeSource={RelativeSource AncestorType=Window}}">

        <DataGrid.ContextMenu>
            <ContextMenu DataContext="{Binding PlacementTarget.DataContext,
                                               RelativeSource={RelativeSource Self}}">
                <MenuItem Header="Delete Transaction"
                          Command="{Binding DeleteTransactionCommand}"/>
            </ContextMenu>
        </DataGrid.ContextMenu>

        <DataGrid.RowStyle>
            <Style TargetType="DataGridRow">
                <Setter Property="DetailsVisibility"
                        Value="Collapsed"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsExpanded}"
                            Value="True">
                        <Setter Property="DetailsVisibility"
                                Value="Visible"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </DataGrid.RowStyle>

        <DataGrid.RowDetailsTemplate>
            <DataTemplate>
                <Border Margin="42,0,12,10"
                        Padding="12"
                        Background="#F8FAFC"
                        BorderBrush="#CBD5E1"
                        BorderThickness="1"
                        CornerRadius="4">

                    <StackPanel>
                        <TextBlock Text="Accounting Coding"
                                   FontWeight="SemiBold"
                                   FontSize="14"
                                   Margin="0,0,0,8"/>

                        <Grid Grid.IsSharedSizeScope="True">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0"
                                  Margin="0,0,0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="90"
                                                      SharedSizeGroup="CodeColumn"/>
                                    <ColumnDefinition Width="170"
                                                      SharedSizeGroup="CodeNameColumn"/>
                                    <ColumnDefinition Width="*"
                                                      SharedSizeGroup="DescriptionColumn"/>
                                    <ColumnDefinition Width="110"
                                                      SharedSizeGroup="AmountColumn"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0"
                                           Text="Code"
                                           Style="{StaticResource DetailHeaderTextStyle}"/>
                                <TextBlock Grid.Column="1"
                                           Text="Code Name"
                                           Style="{StaticResource DetailHeaderTextStyle}"/>
                                <TextBlock Grid.Column="2"
                                           Text="Description"
                                           Style="{StaticResource DetailHeaderTextStyle}"/>
                                <TextBlock Grid.Column="3"
                                           Text="Amount"
                                           TextAlignment="Right"
                                           Style="{StaticResource DetailHeaderTextStyle}"/>
                            </Grid>

                            <ItemsControl Grid.Row="1"
                                          ItemsSource="{Binding LineItems}">
                                <ItemsControl.Style>
                                    <Style TargetType="ItemsControl">
                                        <Setter Property="Visibility"
                                                Value="Visible"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding HasLineItems}"
                                                         Value="False">
                                                <Setter Property="Visibility"
                                                        Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ItemsControl.Style>

                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="#E5E7EB"
                                                BorderThickness="0,0,0,1">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="90"
                                                                      SharedSizeGroup="CodeColumn"/>
                                                    <ColumnDefinition Width="170"
                                                                      SharedSizeGroup="CodeNameColumn"/>
                                                    <ColumnDefinition Width="*"
                                                                      SharedSizeGroup="DescriptionColumn"/>
                                                    <ColumnDefinition Width="110"
                                                                      SharedSizeGroup="AmountColumn"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0"
                                                           Text="{Binding Code,
                                                                          TargetNullValue=Uncoded,
                                                                          FallbackValue=Uncoded}"
                                                           FontWeight="SemiBold"
                                                           Style="{StaticResource DetailValueTextStyle}"/>
                                                <TextBlock Grid.Column="1"
                                                           Text="{Binding CodeName}"
                                                           Style="{StaticResource DetailValueTextStyle}"/>
                                                <TextBlock Grid.Column="2"
                                                           Text="{Binding Description}"
                                                           Style="{StaticResource DetailValueTextStyle}"/>
                                                <TextBlock Grid.Column="3"
                                                           Text="{Binding Total,
                                                                          StringFormat=C}"
                                                           TextAlignment="Right"
                                                           Style="{StaticResource DetailValueTextStyle}"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>

                        <TextBlock Text="No accounting coding lines have been added."
                                   Foreground="#92400E"
                                   FontStyle="Italic"
                                   Margin="4,6,4,10">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility"
                                            Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasLineItems}"
                                                     Value="False">
                                            <Setter Property="Visibility"
                                                    Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <Separator Margin="0,8,0,8"/>

                        <Grid HorizontalAlignment="Right">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="110"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Row="0"
                                       Grid.Column="0"
                                       Text="Transaction total:"
                                       Margin="0,2,12,2"/>
                            <TextBlock Grid.Row="0"
                                       Grid.Column="1"
                                       Text="{Binding Total, StringFormat=C}"
                                       TextAlignment="Right"
                                       Margin="0,2"/>

                            <TextBlock Grid.Row="1"
                                       Grid.Column="0"
                                       Text="Allocated:"
                                       Margin="0,2,12,2"/>
                            <TextBlock Grid.Row="1"
                                       Grid.Column="1"
                                       Text="{Binding AllocatedTotal, StringFormat=C}"
                                       TextAlignment="Right"
                                       Margin="0,2"/>

                            <TextBlock Grid.Row="2"
                                       Grid.Column="0"
                                       Text="Remaining:"
                                       FontWeight="SemiBold"
                                       Margin="0,2,12,2"/>
                            <TextBlock Grid.Row="2"
                                       Grid.Column="1"
                                       Text="{Binding RemainingTotal, StringFormat=C}"
                                       FontWeight="SemiBold"
                                       TextAlignment="Right"
                                       Margin="0,2">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground"
                                                Value="#B45309"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsAllocationBalanced}"
                                                         Value="True">
                                                <Setter Property="Foreground"
                                                        Value="#166534"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                    </StackPanel>
                </Border>
            </DataTemplate>
        </DataGrid.RowDetailsTemplate>

        <DataGrid.Columns>
            <DataGridTemplateColumn Width="34">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <ToggleButton IsChecked="{Binding IsExpanded,
                                                         Mode=TwoWay,
                                                         UpdateSourceTrigger=PropertyChanged}"
                                      Width="24"
                                      Height="24"
                                      Padding="0"
                                      ToolTip="Show or hide accounting coding">
                            <TextBlock FontSize="10">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text"
                                                Value="▶"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsExpanded}"
                                                         Value="True">
                                                <Setter Property="Text"
                                                        Value="▼"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </ToggleButton>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>

            <DataGridTextColumn Header="Date of Payment"
                                Binding="{Binding PaymentDate,
                                                  StringFormat={}{0:dd/MM/yyyy}}"
                                Width="105"/>

            <DataGridTextColumn Header="Code(s)"
                                Binding="{Binding CodesSummary}"
                                Width="130">
                <DataGridTextColumn.ElementStyle>
                    <Style TargetType="TextBlock">
                        <Setter Property="ToolTip"
                                Value="{Binding CodesToolTip}"/>
                        <Setter Property="TextTrimming"
                                Value="CharacterEllipsis"/>
                        <Setter Property="VerticalAlignment"
                                Value="Center"/>
                    </Style>
                </DataGridTextColumn.ElementStyle>
            </DataGridTextColumn>

            <DataGridTextColumn Header="Receipt Date"
                                Binding="{Binding ReceiptDate,
                                                  StringFormat={}{0:dd/MM/yyyy}}"
                                Width="100"/>

            <DataGridTextColumn Header="Receipt No."
                                Binding="{Binding DocumentNumber}"
                                Width="125"/>

            <DataGridTextColumn Header="Company"
                                Binding="{Binding BusinessName}"
                                Width="150"/>

            <DataGridTextColumn Header="Description"
                                Binding="{Binding Description}"
                                Width="*"/>

            <DataGridTextColumn Header="Total"
                                Binding="{Binding Total, StringFormat=C}"
                                Width="100">
                <DataGridTextColumn.ElementStyle>
                    <Style TargetType="TextBlock">
                        <Setter Property="TextAlignment"
                                Value="Right"/>
                        <Setter Property="VerticalAlignment"
                                Value="Center"/>
                        <Setter Property="Margin"
                                Value="0,0,6,0"/>
                    </Style>
                </DataGridTextColumn.ElementStyle>
            </DataGridTextColumn>

            <DataGridTemplateColumn Header="VAT"
                                    Width="72">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <Border ToolTip="{Binding VatIssuesToolTip}">
                            <Border.Style>
                                <Style TargetType="Border"
                                       BasedOn="{StaticResource StatusBadgeBorderStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsVatReady}"
                                                     Value="True">
                                            <Setter Property="Background"
                                                    Value="#DCFCE7"/>
                                            <Setter Property="BorderBrush"
                                                    Value="#22C55E"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <TextBlock Text="{Binding VatStatusText}">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock"
                                           BasedOn="{StaticResource StatusBadgeTextStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsVatReady}"
                                                         Value="True">
                                                <Setter Property="Foreground"
                                                        Value="#166534"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Border>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>

            <DataGridTemplateColumn Header="Tax"
                                    Width="72">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <Border ToolTip="{Binding TaxIssuesToolTip}">
                            <Border.Style>
                                <Style TargetType="Border"
                                       BasedOn="{StaticResource StatusBadgeBorderStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsTaxReady}"
                                                     Value="True">
                                            <Setter Property="Background"
                                                    Value="#DCFCE7"/>
                                            <Setter Property="BorderBrush"
                                                    Value="#22C55E"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <TextBlock Text="{Binding TaxStatusText}">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock"
                                           BasedOn="{StaticResource StatusBadgeTextStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsTaxReady}"
                                                         Value="True">
                                                <Setter Property="Foreground"
                                                        Value="#166534"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Border>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
        </DataGrid.Columns>
    </DataGrid>
</UserControl>
