<UserControl x:Class="FarmBooks.UI.Views.ExpenseListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <DataGrid
        ItemsSource="{Binding ExpenseList.Expenses}"
        SelectedItem="{Binding SelectedExpense}"
        AutoGenerateColumns="False"
        CanUserAddRows="False"
        IsReadOnly="True"
        SelectionMode="Single"
        SelectionUnit="FullRow"
        GridLinesVisibility="Horizontal"
        HeadersVisibility="Column"
        AlternatingRowBackground="#F9FAFB">

        <DataGrid.Columns>
            <DataGridTextColumn Header="Expense Date"
                                Binding="{Binding ExpenseDate, StringFormat=d}"/>
            <DataGridTextColumn Header="Paid Date"
                                Binding="{Binding PaidDate, StringFormat=d}"/>
            <DataGridTextColumn Header="Source"
                                Binding="{Binding SourceType}"/>
            <DataGridTextColumn Header="Business Name"
                                Binding="{Binding BusinessName}"
                                Width="160"/>
            <DataGridTextColumn Header="Description"
                                Binding="{Binding Description}"
                                Width="*"/>
            <DataGridTextColumn Header="Total"
                                Binding="{Binding Total, StringFormat=C}"/>
            <DataGridTextColumn Header="Status"
                                Binding="{Binding Status, Mode=OneWay}"/>
        </DataGrid.Columns>
    </DataGrid>
</UserControl>