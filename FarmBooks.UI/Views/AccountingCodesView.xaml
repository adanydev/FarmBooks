<UserControl x:Class="FarmBooks.UI.Views.AccountingCodesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Margin="0,0,0,12">
            <TextBlock Text="Accounting Codes"
                       FontSize="28"
                       FontWeight="SemiBold"
                       DockPanel.Dock="Left"/>

            <StackPanel Orientation="Horizontal"
                        DockPanel.Dock="Right">
                <TextBlock Text="{Binding Message}"
                           VerticalAlignment="Center"
                           Foreground="#6B7280"
                           Margin="0,0,12,0"/>

                <Button Content="New Code"
                        Width="100"
                        Height="34"
                        Margin="0,0,8,0"
                        Command="{Binding NewCodeCommand}"/>

                <Button Content="Save"
                        Width="80"
                        Height="34"
                        Command="{Binding SaveCommand}"/>
            </StackPanel>
        </DockPanel>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Codes}"
                  SelectedItem="{Binding SelectedCode}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  AlternatingRowBackground="#F9FAFB">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Code"
                                    Binding="{Binding Code, UpdateSourceTrigger=PropertyChanged}"
                                    Width="120"/>

                <DataGridTextColumn Header="Name"
                                    Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                                    Width="220"/>

                <DataGridTextColumn Header="Description"
                                    Binding="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                                    Width="*"/>

                <DataGridCheckBoxColumn Header="Active"
                                        Binding="{Binding IsActive, UpdateSourceTrigger=PropertyChanged}"
                                        Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>