<UserControl x:Class="FarmBooks.UI.Views.ExpenseEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:FarmBooks.UI.Views">

    <Border BorderBrush="#D1D5DB"
            BorderThickness="1"
            CornerRadius="4"
            Background="White">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Fixed editor toolbar -->
            <Grid Grid.Row="0"
                  Margin="16,14,16,12">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0"
                            Margin="0,0,12,0">

                    <TextBlock Text="{Binding Message}"
                               Foreground="#6B7280"/>

                    <TextBlock Text="{Binding ValidationMessage}"
                               Foreground="#B91C1C"
                               TextWrapping="Wrap"
                               Margin="0,3,0,0"/>
                </StackPanel>

                <Button Grid.Column="1"
                        Content="Save Expense"
                        Width="115"
                        Height="32"
                        VerticalAlignment="Top"
                        Command="{Binding SaveCommand}"/>
            </Grid>

            <!-- Scrollable editor content -->
            <ScrollViewer Grid.Row="1"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">

                <StackPanel Margin="16,0,16,16">

                    <Border Padding="10"
                            Margin="0,0,0,12"
                            Background="#FFF7ED"
                            BorderBrush="#FDBA74"
                            BorderThickness="1"
                            CornerRadius="4"
                            ToolTip="{Binding OutstandingItemsToolTip}">

                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Visibility"
                                        Value="Collapsed"/>

                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasOutstandingItems}"
                                                 Value="True">
                                        <Setter Property="Visibility"
                                                Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>

                        <StackPanel>
                            <TextBlock Text="Outstanding Items"
                                       FontWeight="SemiBold"/>

                            <ItemsControl ItemsSource="{Binding OutstandingItems}"
                                          Margin="0,4,0,0">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Message,
                                                                  StringFormat=• {0}}"
                                                   TextWrapping="Wrap"
                                                   Margin="0,2,0,0"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                    <views:ExpenseDetailsView DataContext="{Binding Details}"/>

                    <Separator Margin="0,12,0,12"/>

                    <views:ExpenseVatView DataContext="{Binding Details}"/>

                    <Separator Margin="0,12,0,12"/>

                    <views:ExpenseLineItemsView DataContext="{Binding LineItems}"/>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>